# 企业级发布系统 · PoC 验证与验收方案

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-09 |
| 配套文档 | 《选型与设计调研文档》《详细设计文档》《详细设计文档（KubeVela 选项B）》 |
| 本文档范围 | A1 KubeVela PoC 验证清单 + A6 性能验收标准 + 双路线对比 PoC 组织 |
| 约束 | 本项目无信创要求，不涉及国产化适配验证 |

---

## 目录

1. [PoC 目标与原则](#1-poc-目标与原则)
2. [环境与数据准备](#2-环境与数据准备)
3. [A 路线 PoC 清单（ArgoCD + Rollouts）](#3-a-路线-poc-清单)
4. [B 路线 PoC 清单（KubeVela）](#4-b-路线-poc-清单kubvela)
5. [性能验收标准（量化判据）](#5-性能验收标准量化判据)
6. [双路线对比评分](#6-双路线对比评分)
7. [组织与排期](#7-组织与排期)
8. [风险与退出机制](#8-风险与退出机制)

---

## 1. PoC 目标与原则

### 1.1 目标

在正式投入研发前，用 **2 周**时间回答三个问题：

1. **规模可行性**：单分片 2,000 应用（全量 10,000 的 1/5）下，两条路线的调和延迟、发布并发、资源消耗是否达标；
2. **集成可行性**：与自建发布单/审批流的集成方式是否顺畅（这是两条路线共同的上层，必须验证打通）;
3. **路线决策依据**：A（ArgoCD+Rollouts）vs B（KubeVela）的量化对比数据，支撑最终选型。

### 1.2 原则

- **同场景对比**：两条路线跑完全相同的应用集、发布场景、压测脚本，结果才可比；
- **PoC ≠ 生产**：不追求高可用部署，单实例即可，聚焦功能与性能；
- **数据说话**：所有结论必须附压测原始数据（Prometheus 导出），不接受"感觉差不多"；
- **有退出机制**：任一路线出现颠覆性问题（如网络模型不通），立即标记并切换另一路线，不恋战。

---

## 2. 环境与数据准备

### 2.1 集群规划

| 角色 | 数量 | 规格 | 用途 |
|------|------|------|------|
| 控制面集群 | 1 | 8 核 / 16 GiB / 3 节点 | 部署分片控制组件（vela-core 或 ArgoCD） |
| 业务集群 | 3 | 8 核 / 16 GiB / 3 节点 | 承接应用负载；其中 1 个模拟跨机房（不同可用区） |
| 负载机 | 1 | 4 核 / 8 GiB | 跑压测脚本与模拟 CI 调用 |

> 小规模起步可用 k3d/kind 模拟业务集群（参考官方 200 集群 k3d 方案），但**性能验收必须在真实集群上复测**。

### 2.2 应用生成器

编写 `app-generator` 工具（PoC 自研，约 1 人日）：

- 输入：应用数量 N、副本数分布、资源规格分布；
- 输出：N 个应用的清单（A 路线生成 Deployment/Service YAML 入 Git；B 路线生成 Application YAML 入 Git）；
- 分布设计：80% 小应用（1 Deployment）、15% 中应用（Deployment+ConfigMap+Secret）、5% 大应用（3 Deployment+多配置），对齐官方压测的应用形态；
- 镜像用占位镜像（如 nginx），避免真实构建干扰。

### 2.3 模拟 CI

用脚本模拟 CI 高频调用发布 API（创建发布单 → 推进批次），并发度可调，用于发布并发压测。

---

## 3. A 路线 PoC 清单

**架构**：分片 ArgoCD（状态收敛）+ Argo Rollouts（发布策略）+ Argo Workflows（编排）。

| # | 验证项 | 方法 | 通过标准 |
|---|--------|------|----------|
| A1 | ArgoCD 分片 | `ARGOCD_CONTROLLER_REPLICAS=2`，2000 应用按分片标签分布 | 两分片负载均衡，无单片过载 |
| A2 | ApplicationSet 生成 | 按目录生成 2000 个 Application | 全量生成 ≤ 10 分钟，无遗漏 |
| A3 | repo-server 渲染性能 | 2000 应用全量 manifest 渲染 | 开启缓存后单应用渲染 ≤ 2s |
| A4 | Rollouts 金丝雀 | 10%→50%→100% 分批 + Analysis 门禁 | 每批 Analysis 生效，失败自动回滚 |
| A5 | Rollouts 蓝绿 | 蓝绿切换 | 切换零中断 |
| A6 | onExit 回滚钩子 | 发布中途人为注入失败 | 自动回滚到上一版本，状态正确 |
| A7 | Argo Workflows 编排 | 多步工作流（含 suspend 审批） | 可暂停/恢复，状态落库 |
| A8 | 漂移检测 | 手动改集群内资源 | 下一轮调和自动纠正，延迟 ≤ 调和周期 |
| A9 | 与发布单集成 | 工作流更新 Git → ArgoCD 同步 → 回写发布单状态 | 端到端状态一致 |

---

## 4. B 路线 PoC 清单（KubeVela）

**架构**：vela-core（控制器分片）+ cluster-gateway（多集群）+ CUE 定义体系，GitOps 由 ArgoCD 同步 Application CR。

| # | 验证项 | 方法 | 通过标准 | 结果 |
|---|--------|------|----------|------|
| B1 | 安装与版本 | Helm 安装 vela-core v1.11.0 + cluster-gateway addon | 安装顺利，组件健康 | ☐ |
| B2 | **CUE 定义开发成本** | 平台团队编写 5 个企业 ComponentDefinition + 5 个 TraitDefinition | 单定义 ≤ 0.5 人日；团队 1 周内上手 CUE | ☐ |
| B3 | defkit 工具链 | 用 v1.11 defkit Go SDK 生成/校验定义 | 可用，降低手写 CUE 成本 | ☐ |
| B4 | Application 发布 | 提交含 component+trait 的 Application | 正确渲染出底层资源 | ☐ |
| B5 | **多集群分发** | topology policy 分发到 3 个业务集群 | 分发正确，状态可查 | ☐ |
| B6 | **网络可达性** | 验证 Hub→成员集群 apiserver 连通（含跨可用区） | 全部可达；不可达场景验证降级方案 | ☐ |
| B7 | cluster-gateway 容量 | 并发经 gateway 下发 | 延迟 ≤ 50ms（同机房） | ☐ |
| B8 | **控制器分片** | 2 个 vela-core 分片 + 应用分片标签 | 负载按标签正确路由 | ☐ |
| B9 | workflow 审批联动 | workflow suspend + 外部回调恢复 | 自建审批系统可驱动恢复 | ☐ |
| B10 | GitOps 集成 | ArgoCD 同步 Application CR → vela-core 渲染 | 端到端打通，状态一致 | ☐ |
| B11 | override 环境差异 | 同一 Application 模板 + override policy 出多环境 | 环境差异正确覆盖 | ☐ |
| B12 | 版本升级 | v1.10 → v1.11 升级演练 | 平滑升级，存量 Application 不受影响 | ☐ |
| B13 | 与发布单集成 | 发布单驱动 Application 镜像更新 → 多批次 | 与自建流程集成顺畅 | ☐ |
| B14 | 文档/社区响应 | 记录遇到的问题，提交社区 issue | 响应时效可接受（≤ 3 工作日） | ☐ |

> **B6 是 B 路线的一票否决项**：若企业网络无法打通 Hub→成员集群且无可行降级，B 路线直接出局。

---

## 5. 性能验收标准（量化判据）

> 统一基线场景：**单分片 2,000 应用 / 3 业务集群**。两条路线用同一场景压测。

### 5.1 核心指标与阈值

| 指标 | 测试方法 | 达标阈值 | 红线（超出即不通过） |
|------|----------|----------|----------------------|
| 稳态调和延迟 P99 | 发布后到集群状态 Synced 的时间 | ≤ 10 s | > 30 s |
| 全量一轮调和周期 | 2000 应用完整对账一轮 | ≤ 5 min | > 10 min |
| 发布并发 | 模拟 CI 并发提交发布单 | 50 并发/分片不积压（排队延迟 < 30s） | 30 并发即积压 |
| 单应用发布时长 | 20 副本应用滚动发布（不含审批等待） | ≤ 5 min | > 10 min |
| 金丝雀分批准确性 | 10%/50%/100% 流量/副本比例 | 误差 ≤ 5% | 比例错误或顺序错乱 |
| 控制面资源（稳态） | 2000 应用稳态 CPU/内存 | ≤ 4 核 / 8 GiB（每分片） | > 8 核 / 16 GiB |
| 发布 API 延迟 P99 | 创建/查询发布单接口 | ≤ 500 ms | > 1 s |
| Git 渲染 | 单应用清单 clone+render | ≤ 10 s | > 30 s |
| 控制面故障恢复 | kill 控制器后恢复时间 | < 5 min | > 15 min |
| 故障隔离 | 控制面故障时存量应用 | 不受影响（无重启/漂移） | 存量应用受影响 |

### 5.2 判定规则

| 结果 | 条件 |
|------|------|
| ✅ 通过 | 全部指标达到"达标阈值" |
| ⚠️ 有条件通过 | 个别指标处于达标与红线之间，且有明确优化路径与复测计划 |
| ❌ 不通过 | 任一指标突破红线，或一票否决项（B6）失败 |

### 5.3 数据留存要求

- 所有压测保留 Prometheus 原始数据与 Grafana 截图；
- 每个指标记录：测试时间、环境配置、原始值、判定结果；
- 输出《PoC 压测报告》作为选型决策附件。

---

## 6. 双路线对比评分

PoC 结束后按以下维度打分（每项 1–5 分），加权得出推荐路线：

| 维度 | 权重 | A 路线 | B 路线 | 说明 |
|------|------|--------|--------|------|
| 性能达标度 | 25% | | | 5.1 指标达成情况 |
| 集成顺畅度 | 20% | | | 与发布单/审批流集成的工作量与坑 |
| 自研工作量节省 | 20% | | | B 省抽象层/渲染层/多集群接入 |
| 技术栈成本 | 15% | | | CUE 学习曲线（B 扣分项） |
| 网络模型适配 | 10% | | | Agent vs Gateway 与企业网络匹配 |
| 生态与可持续性 | 10% | | | 社区活跃度、案例、锁定风险 |

> 加权总分差 < 10% 视为持平，此时默认选 A（ArgoCD 生态规模验证更充分、无 CUE 成本）。

---

## 7. 组织与排期

### 7.1 人力

| 角色 | 人数 | 职责 |
|------|------|------|
| 平台工程师 | 2 | 环境搭建、两路线部署、压测 |
| 后端工程师 | 1 | app-generator、模拟 CI、发布单集成 |
| 架构师 | 1（兼） | 方案设计、评分、决策 |

### 7.2 排期（2 周）

| 阶段 | 天数 | 内容 |
|------|------|------|
| 环境准备 | 2 | 集群搭建、app-generator、模拟 CI |
| 功能验证 | 4 | A/B 两路线功能清单逐项验证 |
| 性能压测 | 3 | 按 5.1 指标压测，留存数据 |
| 对比评分与报告 | 1 | 打分、输出《PoC 报告》与选型建议 |

### 7.3 关键里程碑

- **M1（第 2 天）**：环境就绪，app-generator 产出 2000 应用清单；
- **M2（第 6 天）**：两路线功能验证完成，B6 网络可行性结论明确；
- **M3（第 9 天）**：性能压测完成，原始数据齐全；
- **M4（第 10 天）**：《PoC 报告》+ 选型建议评审。

---

## 8. 风险与退出机制

| 风险 | 应对 |
|------|------|
| B6 网络不可达 | 一票否决，转 A 路线 |
| CUE 上手慢拖累 B 进度 | 设 3 天观察期，超时则 B 的"技术栈成本"项扣分并按现有数据评分 |
| 压测环境资源不足 | 优先保核心指标（调和延迟/发布并发），其余降级为估算 |
| 两路线都达标且分数接近 | 按 6 节规则默认选 A，避免决策拖延 |
| PoC 发现颠覆性缺陷 | 立即记录并评估是否影响架构假设，必要时回炉设计 |

---

*文档结束 · PoC 通过后，据《PoC 报告》锁定路线，进入一期研发。*
