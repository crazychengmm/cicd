# 企业级发布系统 · 非功能设计文档

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-09 |
| 配套文档 | 《选型与设计调研文档》《详细设计文档》 |
| 本文档范围 | A3 安全设计 + A4 高可用与容灾设计 + A5 可观测性设计 |
| 约束 | 本项目无信创要求 |

---

## 目录

1. [安全设计](#1-安全设计)
2. [高可用与容灾设计](#2-高可用与容灾设计)
3. [可观测性设计](#3-可观测性设计)

---

# 1. 安全设计

## 1.1 安全目标与威胁模型

| 保护对象 | 主要威胁 | 防护目标 |
|----------|----------|----------|
| 发布权限 | 越权发布、未审批上线 | 最小权限 + 强制审批 |
| 凭据 | 镜像仓库/集群凭据泄露 | 集中管理、短时效、可轮换 |
| 制品 | 镜像被篡改、恶意镜像 | 签名 + 准入校验 |
| 配置/清单 | Git 中明文密钥、清单被篡改 | 加密 + 评审 + 签名 |
| 操作行为 | 抵赖、无法追溯 | 全量审计 |

## 1.2 认证与授权

### 认证
- **人员**：SSO/OIDC 统一认证，不维护独立账号体系；
- **服务间**：发布系统各组件用服务账号 + mTLS；
- **API**：对外 OpenAPI 用短时效 token，支持按调用方限流。

### RBAC 授权模型

三个维度交叉：**角色 × 资源范围 × 操作**。

| 角色 | BU 范围 | 环境范围 | 典型操作 |
|------|---------|----------|----------|
| 平台管理员 | 全部 | 全部 | 定义管理、分片管理、封网设置 |
| BU 管理员 | 本 BU | 全部 | 本 BU 应用管理、审批配置 |
| 开发者 | 本 BU 应用 | dev/test/staging | 发起发布（生产需审批） |
| 审批人 | 本 BU | prod | 审批发布单 |
| 只读 | 按授权 | 按授权 | 查看发布单/状态 |

**与 K8s RBAC 映射**：控制面在成员集群使用**最小权限 ServiceAccount**（仅发布系统所需 verb），不使用 cluster-admin；按分片隔离命名空间。

## 1.3 密钥与凭据管理

| 凭据类型 | 管理方式 |
|----------|----------|
| 镜像仓库凭据 | 集群内 Secret + 定期轮换；优先用云厂商免密拉取（如 ACR 免密插件） |
| 集群凭据（kubeconfig/证书） | 集中存储于控制面 Secret 或 KMS/Vault；支持证书轮换；选项B 用 KubeVela cluster secret |
| Git 凭据 | CI/CD 专用 deploy token，只读/读写最小化 |
| 应用配置中的敏感值 | **不入 Git 明文**：用外部引用（External Secrets / SOPS / 配置中心），渲染时注入 |

**原则**：Git 中永远不出现明文密钥；所有 Secret 变更有审计。

## 1.4 供应链安全

1. **镜像签名**：CI 构建后用 cosign 对镜像签名；
2. **准入校验**：成员集群部署准入策略（如 Kyverno/OPA），拒绝未签名/来源不可信镜像；
3. **基础镜像治理**：统一可信基础镜像仓库，定期扫描漏洞；
4. **定义变更评审**：CUE/Helm 模板（平台层"代码"）变更走 PR 评审 + CI 校验，防止平台层注入恶意渲染逻辑。

## 1.5 网络安全

- 控制面组件间、控制面→成员集群全程 **TLS**，内部可选 mTLS；
- 发布 API 经网关暴露，配限流、WAF、防重放；
- 成员集群 apiserver 不对公网开放，仅控制面可达网段访问；
- 选项B 的 Hub→成员集群通道走专线/内网，cluster-gateway 凭据最小化。

## 1.6 审计与合规

- **全量操作审计**：发布单创建/审批/批次推进/回滚、定义变更、凭据访问，全部落审计日志；
- **关联发布单号**：每条审计可追溯到具体发布单与操作人；
- **留存策略**：审计日志在线 ≥ 180 天，归档 ≥ 1 年（按企业合规要求调整）；
- **不可篡改**：审计存储只增不删，写入后不可修改。

---

# 2. 高可用与容灾设计

## 2.1 可用性目标

| 对象 | 目标 | 说明 |
|------|------|------|
| 控制面 API | 99.9% | 月度不可用 < 43 分钟 |
| 发布操作 | 控制面故障时**存量应用不受影响** | 关键设计：发布系统与业务运行时解耦 |
| 数据 | RPO ≤ 5 min，RTO ≤ 30 min | 见 2.4 |

> **核心原则**：发布系统是"交付通道"而非"运行时依赖"。控制面短暂不可用时，已部署应用继续运行，只是暂停新发布——这通过 GitOps/Pull 模式天然获得。

## 2.2 组件高可用矩阵

| 组件 | HA 方式 | 副本 | 故障影响 |
|------|---------|------|----------|
| shard-api-server | 无状态多副本 + LB | ≥3 | 单实例故障无感 |
| shard-controller | leader-election 主备 | 2 | 主故障秒级切换 |
| Argo Workflows controller | HA 模式 | 2 | 工作流可重入恢复 |
| ArgoCD / vela-core | HA / 控制器分片 | ≥2 | 收敛延迟上升但不丢状态 |
| Kafka | 3 节点，副本因子 3 | 3 | 单节点故障无感 |
| MySQL | 一主两从 + 半同步复制 | 3 | 主从切换 < 1 min |
| Git 仓库 | 用 GitLab HA 或托管服务 | — | 依赖 Git 自身 HA |

**无状态与幂等**：所有执行动作幂等（工作流重试是常态），控制器 reconcile 可从任意中间态恢复。

## 2.3 数据层设计

- **MySQL**：一主两从 + 半同步，自动故障切换（如 Orchestrator/云 RDS）；
- **备份**：每日全量 + binlog 增量，备份跨机房存储；
- **恢复演练**：每季度一次备份恢复演练，验证 RTO；
- **etcd 保护**（延续防坑清单）：大对象不进 CRD，降低 K8s 控制面故障面。

## 2.4 RPO / RTO 设计

| 场景 | RPO | RTO | 机制 |
|------|-----|-----|------|
| MySQL 主库故障 | ≤ 5 min（半同步） | ≤ 1 min（自动切换） | 半同步复制 + 自动 failover |
| Git 仓库故障 | 0（多副本） | ≤ 10 min | Git HA / 托管服务 |
| 控制面集群整体故障 | ≤ 5 min | ≤ 30 min | 跨机房备控制面 + Git 重建 |
| 单成员集群故障 | — | 业务侧自愈 | 业务集群多可用区，非发布系统职责 |

## 2.5 跨机房容灾

- **控制面**：主机房 active，备机房 standby（或双活 + 流量切换）；备控制面可从 Git + DB 备份完整重建（GitOps 的声明式特性使重建可行）；
- **Git**：跨机房副本（Git 托管服务自带或自建镜像同步）；
- **业务集群**：本身跨多可用区/多机房部署，单集群故障由多集群分发（topology/Karmada）兜底。

## 2.6 故障分级与演练

| 级别 | 定义 | 响应 |
|------|------|------|
| P1 | 发布完全不可用 / 数据丢失风险 | 立即响应，15 min 内启动恢复 |
| P2 | 发布能力降级（部分分片不可用） | 30 min 内响应 |
| P3 | 单组件异常，自动恢复 | 记录跟进 |

**混沌演练计划（季度）**：
1. kill 控制器，验证 leader 切换与 reconcile 恢复；
2. MySQL 主从切换，验证 RTO；
3. Kafka 单节点下线；
4. 成员集群断连，验证控制面不雪崩、其他集群不受影响。

---

# 3. 可观测性设计

## 3.1 指标分层

```
业务层   发布单成功率 / 批次时长 / 回滚率 / 封网拦截数
   ▲
组件层   调和延迟 / 工作流队列深度 / gateway 延迟 / repo-server 渲染耗时
   ▲
基础层   K8s apiserver / etcd / 节点资源 / 网络延迟
```

## 3.2 关键指标清单

| 层 | 指标 | 说明 | 告警阈值 |
|----|------|------|----------|
| 业务 | 发布成功率 | 成功发布单/总发布单（滚动 1h） | < 95% |
| 业务 | 回滚率 | 触发回滚/总发布 | > 10% |
| 业务 | 发布单排队时长 | 创建到开始执行 | P99 > 5 min |
| 业务 | 封网拦截数 | 封网期被拦截的发布 | 仅统计，不告警 |
| 组件 | 调和延迟 P99 | 发布到集群 Synced | > 10s（对齐 PoC 阈值） |
| 组件 | 工作流队列深度 | 待处理工作流数 | > 100 |
| 组件 | cluster-gateway 延迟 | Hub→成员集群请求 | P99 > 100ms |
| 组件 | repo-server 渲染耗时 | 清单渲染 | P99 > 10s |
| 组件 | reconcile 错误率 | 控制器 reconcile 失败 | > 1% |
| 基础 | apiserver 请求延迟 | 控制面 apiserver | P99 > 500ms |
| 基础 | etcd 大小/延迟 | 防 etcd 膨胀 | 大小 > 6GB |

## 3.3 日志

- **统一格式**：所有组件输出结构化 JSON（timestamp/level/trace_id/shard/app/order_id）；
- **分级**：发布动作与审批为 INFO 强制记录，调试为 DEBUG 默认关闭；
- **采集与留存**：统一日志平台，在线 ≥ 7 天，归档 ≥ 90 天；发布单相关日志按 order_id 可检索。

## 3.4 链路追踪

- **全链路 trace_id**：一次发布从 API → Kafka → Argo Workflows → Git → 集群，贯穿同一 trace_id；
- **关联发布单**：trace 与发布单号互查，排障时"从发布单看全链路"；
- 采样：生产默认 10%，失败发布 100% 采样。

## 3.5 告警与值班

- **分级告警**：P1（电话/IM @值班）、P2（IM 工单）、P3（汇总日报）；
- **告警收敛**：同根因告警合并，避免风暴；
- **值班表**：平台团队轮值，告警 SLA 与故障分级对齐（2.6）。

## 3.6 Dashboard

| Dashboard | 受众 | 内容 |
|-----------|------|------|
| 全局总览 | 平台负责人 | 全分片发布量/成功率/Top 故障 |
| 分片详情 | 平台工程师 | 单分片调和/队列/资源 |
| 单发布单 | 开发者/审批人 | 批次进度/门禁结果/链路 |
| 容量水位 | SRE | 控制面资源、etcd、Kafka 水位 |

## 3.7 DORA 度量（发布系统自证价值）

从发布单数据自动计算四项 DORA 指标，作为平台运营报表：

| 指标 | 计算 |
|------|------|
| 部署频率 | 单位时间发布单数 |
| 变更前置时间 | 代码提交 → 生产发布 |
| 变更失败率 | 导致回滚/故障的发布占比 |
| 恢复时间（MTTR） | 故障发布到回滚完成 |

## 3.8 自身存活监控（Dead Man's Switch）

- 发布系统自身也需要被监控：外部探针定期调用发布 API，失败则绕过自身告警链路直接通知；
- 防止"监控系统挂了没人知道"。

---

*文档结束 · 安全/高可用/可观测三部分应在二期随多集群落地同步建设，一期至少先落地 RBAC、审计与关键指标监控。*
